spring:
   profiles:
      active:
        - dev
---
server:
  port: 3001

spring:
  profiles: dev
  application:
      name: microconfig-test-client1

eureka:
 client:
  service-url:
    defaultZone: http://localhost:7001/eureka
---
server:
  port: 3002

spring:
  profiles: beta
  application:
      name: microconfig-test-client2

eureka:
 client:
  service-url:
    defaultZone: http://localhost:7001/eureka