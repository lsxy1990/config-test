spring:
   profiles:
      active:
        - dev
---
server:
  port: 3001

spring:
  profiles: dev
  application:
      name: microconfig-test-client

eureka:
 client:
  service-url:
    defaultZone: http://localhost:7001/eureka
  register-with-eureka: false
---
server:
  port: 3002

spring:
  profiles: beta
  application:
      name: microconfig-test-client

eureka:
 client:
  service-url:
    defaultZone: http://localhost:7001/eureka
  register-with-eureka: false